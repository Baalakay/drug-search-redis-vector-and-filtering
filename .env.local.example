# Local Development Environment Variables
# Copy this file to .env.local and fill in your actual values
# .env.local is gitignored and will not be committed

# Project Configuration
PROJECT_NAME=DAW
SST_STAGE=dev
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=750389970429

# Redis Configuration
# Option 1: Use environment variable (for local dev)
REDIS_HOST=10.0.11.153
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password-here

# Option 2: Let scripts retrieve from AWS Secrets Manager automatically
# (Leave REDIS_PASSWORD unset to use Secrets Manager)

# Database Configuration
# Option 1: Use environment variables (for local dev)
DB_HOST=daw-aurora-dev.cluster-ccbkass84b2d.us-east-1.rds.amazonaws.com
DB_PORT=3306
DB_NAME=fdb
DB_USER=dawadmin
DB_PASSWORD=your-db-password-here

# Option 2: Let scripts retrieve from AWS Secrets Manager automatically
# (Leave DB_PASSWORD unset to use Secrets Manager)
# Secret name format: ${PROJECT_NAME}-DB-Password-${SST_STAGE}

# AWS Resource IDs (non-sensitive, but can be overridden)
VPC_ID=vpc-050fab8a9258195b7
PRIVATE_SUBNET_1=subnet-05ea4d85ade4340db
PRIVATE_SUBNET_2=subnet-07c025dd82ff8355e
LAMBDA_SECURITY_GROUP_ID=sg-0e78f3a483550e499

# Notes:
# - For local development, you can set passwords directly here
# - For AWS environments, leave passwords unset and scripts will use Secrets Manager
# - Never commit .env.local to git (already in .gitignore)
